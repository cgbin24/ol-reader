import{_ as e}from"./app.Dm-7_35L.js";import{p as i,x as a,_ as n}from"./chunks/@vue.D4PtYCMz.js";/* empty css                          */import"./chunks/@vueuse.BWmAHaqk.js";const b=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"mdviewer/XIAOCE/从 0 到 1 实现一套 CI & CD 流程/2 安装 Docker 和 Jenkins：持续构建环境起步.md","filePath":"mdviewer/XIAOCE/从 0 到 1 实现一套 CI & CD 流程/2 安装 Docker 和 Jenkins：持续构建环境起步.md"}'),p={name:"mdviewer/XIAOCE/从 0 到 1 实现一套 CI & CD 流程/2 安装 Docker 和 Jenkins：持续构建环境起步.md"};function d(t,s,c,o,k,l){return a(),i("div",null,s[0]||(s[0]=[n(`<h3 id="前言" tabindex="-1">前言 ​</h3><p>在第一章，我们已经大致了解过 <code>CI/CD</code> 的作用和历史。从这一章起，我们将正式开启搭建一套 <code>CI/CD</code> 流程。在开始之前，首先要学习下贯穿我们小册全程的一个技术：<code>Docker 和 Jenkins</code></p><h3 id="什么是-docker" tabindex="-1">什么是 Docker ​</h3><p><code>Docker</code> 是一个开源的应用容器引擎。开发者可以将自己的应用打包在自己的镜像里面，然后迁移到其他平台的 <code>Docker</code> 中。镜像中可以存放你自己自定义的运行环境，文件，代码，设置等等内容，再也不用担心环境造成的运行问题。镜像共享运行机器的系统内核。</p><p>同样， <code>Docker</code> 也支持跨平台。你的镜像也可以加载在 <code>Windows</code> 和 <code>Linux</code>，实现快速运行和部署。</p><p><img src="https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/0f582c982b59497fa8527d751ca2c2c7~tplv-k3u1fbpfcp-jj-mark:3024:0:0:0:q75.awebp" alt="image.png" loading="lazy"></p><p><code>Docker</code> 的优势在于 快速，轻量，灵活。开发者可以制作一个自己自定义的镜像，也可以使用官方或者其他开发者的镜像来启动一个服务。通过将镜像创建为容器，容器之间相互隔离资源和进程不冲突。但硬件资源又是共享的。 创建的镜像也可以通过文件快速分享，也可以上传到镜像库进行存取和管理。同时 <code>Docker</code> 的镜像有 <code>分层策略</code> ，每次对镜像的更新操作，都会堆叠一个新层。当你拉取 / 推送新版本镜像时，只推送 / 拉取修改的部分。大大加快了镜像的传输效率</p><p>那么 <code>Docker</code> 在 <code>CI/CD</code> 中的作用是什么呢？</p><p><code>Docker</code> 贯穿 <code>CI/CD</code> 中整个流程。作为应用服务的载体有着非常重要的地位。</p><p>我们可以使用 <code>Docker</code> 将应用打包成一个镜像，交给 <code>Kubernetes</code> 去部署在目标服务集群。并且可以将镜像上传到自己的镜像仓库，做好版本分类处理。</p><h3 id="安装-docker" tabindex="-1">安装 <code>Docker</code> ​</h3><p>在了解了 <code>Docker</code> 的作用后，我们来开始安装 <code>Docker</code>。在开始安装之前，需要安装 <code>device-mapper-persistent-data</code> 和 <code>lvm2</code> 两个依赖。</p><p><code>device-mapper-persistent-data</code> 是 <code>Linux</code> 下的一个存储驱动， <code>Linux</code> 上的高级存储技术。 <code>Lvm</code> 的作用则是创建逻辑磁盘分区。这里我们使用 <code>CentOS</code> 的 <code>Yum</code> 包管理器安装两个依赖：</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">yum</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -y</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> yum-utils</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> device-mapper-persistent-data</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> lvm2</span></span></code></pre></div><p>依赖安装完毕后，我们将阿里云的 <code>Docker</code> 镜像源添加进去。可以加速 <code>Docker</code> 的安装。</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> yum-config-manager</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --add-repo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> http://mirrors.aliyun.com/docker-ce/linux/centos/docker-ce.repo</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">yum</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docker-ce</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -y</span></span></code></pre></div><p>安装完毕，我们就可以使用 <code>systemctl</code> 启动来启动 <code>Docker</code> <code>了。systemctl</code> 是 <code>Linux</code> 的进程管理服务命令，他可以帮助我们启动 <code>docker</code></p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">systemctl</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> start</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docker</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">systemctl</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> enable</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docker</span></span></code></pre></div><p>接着执行一下 <code>docker -v</code> ，这条命令可以用来查看 <code>Docker</code> 安装的版本信息。当然也可以帮助我们查看 <code>docker</code> 安装状态。如果正常展示版本信息，代表 <code>Docker</code> 已经安装成功。</p><p><img src="https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/7d98208c9e174be6b0d82acdaa1a2a28~tplv-k3u1fbpfcp-jj-mark:3024:0:0:0:q75.awebp" alt="image.png" loading="lazy"></p><h4 id="配置阿里云镜像源" tabindex="-1">配置阿里云镜像源 ​</h4><p>在 <code>Docker</code> 安装完毕后，之后我们去拉取 <code>docker</code> 镜像时，一般默认会去 <code>docker</code> 官方源拉取镜像。但是国内出海网速实在是太慢，所以选择我们更换为 阿里云镜像仓库 源进行镜像下载加速。</p><p>登录阿里云官网，打开 阿里云容器镜像服务。点击左侧菜单最下面的 镜像加速器 ，选择 <code>CentOS</code> （如下图）。按照官网的提示执行命令，即可更换 <code>docker</code> 镜像源地址。</p><p><img src="https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/ea464ab9fe1941bba822a6131dd631c8~tplv-k3u1fbpfcp-jj-mark:3024:0:0:0:q75.awebp" alt="image.png" loading="lazy"></p><h3 id="安装-jenkins" tabindex="-1">安装 Jenkins ​</h3><p>在安装完 <code>Docker</code> 后，我们只是拥有了一个可以承载服务的载体。想实现自动化构建，还需要安装另一个构建工具 <code>Jenkins</code>。那什么是 <code>Jenkins</code> 呢？</p><p><code>Jenkins</code> 是一个基于 <code>Java</code> 语言开发的持续构建工具平台，主要用于持续、自动的构建/测试你的软件和项目。它可以执行你预先设定好的设置和构建脚本，也可以和 Git 代码库做集成，实现自动触发和定时触发构建。</p><h4 id="_1-安装-openjdk" tabindex="-1">1. 安装 OpenJDK ​</h4><p>因为 <code>Jenkins</code> 是 <code>Java</code> 编写的持续构建平台，所以安装 <code>Java</code> 必不可少。</p><p>在这里，我们选择安装开源的 <code>openjdk</code> 即可。 <code>openjdk</code> 是 <code>SunJDK</code> <strong>一种开源实现</strong>。关于openjdk 是 <code>SunJDK</code> 的具体区别可以看下面的文章了解下。在这我们直接使用 <code>yum</code> 包管理器安装 <code>openjdk</code> 即可。</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">yum</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -y</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> java</span></span></code></pre></div><blockquote><p>www.zhihu.com/question/19…</p></blockquote><h4 id="_2-使用-yum-安装-jenkins" tabindex="-1">2. 使用 Yum 安装 Jenkins ​</h4><p>由于 <code>Yum</code> 源不自带 <code>Jenkins</code> 的安装源，于是我们需要自己导入一份 <code>Jenkins</code> 安装源进行安装。导入后，使用 <code>Yum</code> 命令安装即可。</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> wget</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -O</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /etc/yum.repos.d/jenkins.repo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> https://pkg.jenkins.io/redhat-stable/jenkins.repo</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> rpm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --import</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> https://pkg.jenkins.io/redhat-stable/jenkins.io.key</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">yum</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> jenkins</span></span></code></pre></div><h4 id="_3-启动-jenkins" tabindex="-1">3. 启动 Jenkins ​</h4><p><code>Jenkins</code> 安装后，会将启动命令注册到系统 <code>Service</code> 命令中。所以我们直接使用系统 <code>service</code> 命令启动 <code>Jenkins</code> 即可。在这里，有三个命令可以使用，分别对应 启动 / 重启 / 停止 三个命令。</p><p>在这里，我们直接调用 <code>service jenkins start</code> 启动 <code>Jenkins</code> 即可</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">service</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> jenkins</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> start</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># service jenkins restart restart 重启 Jenkins</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># service jenkins restart stop 停止 Jenkins</span></span></code></pre></div><h4 id="_4-给-jenkins-放行端口" tabindex="-1">4. 给 Jenkins 放行端口 ​</h4><p>在启动 <code>Jenkins</code> 后，此时 <code>Jenkins</code> 会开启它的默认端口 <code>8080</code> 。但由于防火墙限制，我们需要手动让防火墙放行 <code>8080</code> 端口才能对外访问到界面。</p><p>这里我们在 <code>CentOS</code> 下的 <code>firewall-cmd</code> 防火墙添加端口放行规则，添加完后重启防火墙。</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">firewall-cmd</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --zone=public</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --add-port=8080/tcp</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --permanent</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">firewall-cmd</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --zone=public</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --add-port=50000/tcp</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --permanent</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">systemctl</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> reload</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> firewalld</span></span></code></pre></div><p>服务启动后，访问 <code>IP:8080</code> 。如果能够看到以下界面，代表正在启动。 <code>Jenkins</code> 第一次的启动时间一般比较长（视服务器性能而看）</p><p><img src="https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/590e97ce582543dea52a5f90501f5cd1~tplv-k3u1fbpfcp-jj-mark:3024:0:0:0:q75.awebp" alt="image.png" loading="lazy"></p><h4 id="_5-初始化-jenkins-配置" tabindex="-1">5. 初始化 Jenkins 配置 ​</h4><h5 id="_5-1-解锁-jenkins" tabindex="-1">5.1 解锁 Jenkins ​</h5><p>在 <code>Jenkins</code> 启动完成后，会自动跳转至这个界面（下方二图）。这是 <code>Jenkins</code> 的解锁界面，你需要输入存放在服务器的初始解锁密码才能进行下一步操作。</p><p><code>Jenkins</code> 启动后，会生成一个<strong>初始密码</strong>。该密码在服务器的文件内存放，我们可以进入服务器查看密码内容，将密码填写在 <code>Jenkins</code> 的管理员密码输入框内：</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cat</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /var/lib/jenkins/secrets/initialAdminPassword</span></span></code></pre></div><p><img src="https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/80991521841c4fbfbfc24330e820488c~tplv-k3u1fbpfcp-jj-mark:3024:0:0:0:q75.awebp" alt="image.png" loading="lazy"></p><p><img src="https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/48924f5f22e444cf8b61da8466fb228b~tplv-k3u1fbpfcp-jj-mark:3024:0:0:0:q75.awebp" alt="image.png" loading="lazy"></p><p>点击 <code>继续</code> 按钮，解锁 Jenkins。</p><h5 id="_5-2-下载插件" tabindex="-1">5.2 下载插件 ​</h5><p>解锁后就来到了插件下载页面，这一步要下载一些 <code>Jenkins</code> 的功能插件。</p><p><img src="https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/78b36782863c4d1e9d7e06fad09d2dec~tplv-k3u1fbpfcp-jj-mark:3024:0:0:0:q75.awebp" alt="image.png" loading="lazy"></p><p>因为 <code>Jenkins</code> 插件服务器在国外，所以速度不太理想。我们需要更换为清华大学的 <code>Jenkins</code> 插件源后，再安装插件，<strong>所以先不要点安装插件</strong>。</p><p>更换方法很简单。进入服务器，将 <code>/var/lib/jenkins/updates/default.json</code> 内的插件源地址替换成清华大学的源地址，将 google 替换为 baidu 即可。</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sed</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -i</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;s/http:\\/\\/updates.jenkins-ci.org\\/download/https:\\/\\/mirrors.tuna.tsinghua.edu.cn\\/jenkins/g&#39;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /var/lib/jenkins/updates/default.json</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sed</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -i</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;s/http:\\/\\/www.google.com/https:\\/\\/www.baidu.com/g&#39;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /var/lib/jenkins/updates/default.json</span></span></code></pre></div><p><img src="https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/6ec4d971326e4a938596e501dcc92cff~tplv-k3u1fbpfcp-jj-mark:3024:0:0:0:q75.awebp" alt="image.png" loading="lazy"></p><p>接着点击 <code>安装推荐的插件</code> 即可。稍等一会插件安装完毕</p><p><img src="https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/a29085d29f31454d9f4bd0b323f8bc62~tplv-k3u1fbpfcp-jj-mark:3024:0:0:0:q75.awebp" alt="image.png" loading="lazy"></p><h5 id="_6-完成安装" tabindex="-1">6. 完成安装 ​</h5><p>插件安装完毕后，接着是注册管理员账号。按照提示一路配置后，直到看到以下界面代表安装成功</p><p><img src="https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/2bd4415a6da34d05aad501bfd5d5029f~tplv-k3u1fbpfcp-jj-mark:3024:0:0:0:q75.awebp" alt="image.png" loading="lazy"></p><h5 id="_7-测试安装" tabindex="-1">7. 测试安装 ​</h5><p>到这里，我们的 <code>Jenkins</code> 算是启动成功了。但是，我们还需要对 <code>Jenkins</code> 做一点点简单的配置，才可以让它可以构建 <code>docker</code> 镜像。</p><p>我们点击 <code>Jenkins</code> 首页 -&gt; 左侧导航 -&gt; 新建任务 -&gt; <code>Freestyle project</code></p><p><img src="https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/c4bae87d4a1c4494a0615e6f169d5423~tplv-k3u1fbpfcp-jj-mark:3024:0:0:0:q75.awebp" alt="image.png" loading="lazy"></p><p>新建完毕后，找到 <code>构建</code> 一项，选择 <code>增加构建步骤</code>，选择 <code>执行shell</code> ，输入以下命令：</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -v</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pull</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> node:latest</span></span></code></pre></div><p>该命令会去拉取一个 <code>nodejs</code> 稳定版的镜像，我们可以来测试 <code>Docker</code> 的可用性</p><p><img src="https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/198a7f5fbe0f41e8b663f2c2af425fb2~tplv-k3u1fbpfcp-jj-mark:3024:0:0:0:q75.awebp" alt="image.png" loading="lazy"></p><p>保存后，我们点击左侧菜单的<strong>立即构建</strong>， <code>Jenkins</code> 就会开始构建。选择左侧历史记录第一项（最新的一项），点击控制台输出，查看构建日志。</p><p><img src="https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/a633c410f75c4db8be48133a4a516830~tplv-k3u1fbpfcp-jj-mark:3024:0:0:0:q75.awebp" alt="image.png" loading="lazy"></p><p>执行后，我们发现提示无访问权限。这又是什么情况呢？这里就不得不提到 <code>Linux</code> 下的 <code>Unix Socket</code>权限问题了</p><h5 id="_8-unix-socket-权限问题" tabindex="-1">8. Unix Socket 权限问题 ​</h5><p><code>docker</code> 的架构是 <code>C/S</code> 架构。在我们使用 <code>docker</code> 命令时，其实是命令使用 <code>socket</code> 与 <code>docker</code> 的守护进程进行通信，才能正常执行 <code>docker</code> 命令。</p><p>而在 <code>Linux</code> 中， <code>Unix socket</code> 属于 <code>root</code> 用户，因此需要 <code>root</code> 权限才能访问。官方是这么解释的：</p><blockquote><p>Manage Docker as a non-root user The docker daemon binds to a Unix socket instead of a TCP port. By default that Unix socket is owned by the user root and other users can only access it using sudo. The docker daemon always runs as the root user. If you don’t want to use sudo when you use the docker command, create a Unix group called docker and add users to it. When the docker daemon starts, it makes the ownership of the Unix socket read/writable by the docker group.</p></blockquote><p>但是在 <code>docker</code> 中， <code>docker</code> 提供了一个 <code>用户组</code> 的概念。我们可以将执行 <code>Shell</code> 的用户添加到名称为 <code>docker</code> 的用户组，则可以正常执行 <code>docker</code> 命令。</p><p>而在 <code>Jenkins</code> 中执行的终端用户做 <code>jenkins</code> ，所以我们只需要将 <code>jenkins</code> 加入到 <code>docker</code> 用户组即可：</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> groupadd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docker</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">          #新增docker用户组</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gpasswd</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -a</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> jenkins</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docker</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  #将当前用户添加至docker用户组</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">newgrp</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docker</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                 #更新docker用户组</span></span></code></pre></div><p>加入后，<code>重启</code> Jenkins ：</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> service</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> jenkins</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> restart</span></span></code></pre></div><p>重启 Jenkins 后，<code>再次执行脚本</code>。此时执行成功：</p><p><img src="https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/ebb74deda4d04246922c556f74963413~tplv-k3u1fbpfcp-jj-mark:3024:0:0:0:q75.awebp" alt="image.png" loading="lazy"></p><h3 id="结束语" tabindex="-1">结束语 ​</h3><p>到这里为止，我们的基础组件 —— <code>docker</code> 和 <code>Jenkins</code> 就安装完毕了。不过，这只是持续构建的第一步。在下一章，我们会在服务器上安装 <code>Node</code> 环境，并集成 <code>Git</code> 代码库实现对代码的镜像构建。</p><p>如果你有什么疑问，欢迎在评论区提出 👏</p>`,90)]))}const u=e(p,[["render",d]]);export{b as __pageData,u as default};
